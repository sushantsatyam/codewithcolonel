# Day 05 — Hyperparameter tuning

This notebook introduces **hyperparameter tuning** with GridSearchCV on a classic classification dataset.

## What this project shows

- Using a pipeline with scaling + logistic regression
- Defining a parameter grid for tuning
- Selecting the best model with cross-validation

## Folder layout

```
.
├── notebook.ipynb
```

## How I run it

1. Install dependencies:
   ```bash
   pip install scikit-learn
   ```
2. Open the notebook and run the cells.
